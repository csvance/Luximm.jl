var documenterSearchIndex = {"docs":
[{"category":"section","location":"porting/#Porting-Backbones","page":"Porting Backbones","text":"This page is a contributor guide for adding a new timm backbone to Luximm.jl. The bar is numeric parity with timm on the released weights; the rest is mechanics.\n\nIf you are using Claude Code to drive the port, the same workflow is encoded as an agent-facing skill at .claude/skills/timm-to-lux/SKILL.md and loads automatically inside this repo. The skill assumes the Kaimon REPL workflow described in .claude/skills/kaimon-julia/SKILL.md. This page covers the same ground for human contributors without those tools.","title":"Porting Backbones"},{"category":"section","location":"porting/#Acceptance-criteria","page":"Porting Backbones","text":"A new backbone is mergeable when all four hold:\n\nPretrained parity. Forward output of the Lux model with weights loaded via the closure returned by create_pretrained matches timm's forward output on the same input. The bar is two-tier: logits are checked at an absolute max-abs-diff under LOGITS_ATOL = 1f-3, and features (num_classes = 0, and the in_chans = 1 companion) are checked at a relative bar max-abs-diff / max-abs(timm ref) under FEATURES_RTOL = 1f-4. Existing models land well inside this: BiT ResNetV2-50 features around 1.5e-4 absolute (well under the relative bar at typical feature magnitudes), its logits around 2e-5; ConvNeXtV2 atto comparable.\nRandom-init parity. With the Lux model initialized using the same _init_weights recipe timm uses for the family, and timm initialized with the same RNG seed, forward outputs match the same logits and features bars. See _CN2_INIT at src/Models/ConvNeXtV2/Model.jl (truncated_normal(mean = 0f0, std = 0.02f0) mirroring timm's trunc_normal_(std = 0.02)) for a worked example.\nState-dict round-trip. The mapping function consumes every PyTorch state_dict key. Mappings raise on a missing key so silent random-init leaks cannot pass parity by coincidence; see the assertion block at the end of convnextv2_mapping in src/Models/ConvNeXtV2/Model.jl.\nVariant table entry. New variants are registered in <FAMILY>_VARIANTS (src/Models/<Family>/Config.jl) with their HuggingFace repo id and default class count, and listed in the README backbone table.","title":"Acceptance criteria"},{"category":"section","location":"porting/#Why-the-bars-are-1e-3-(logits)-and-1e-4-(features),-not-1e-5","page":"Porting Backbones","text":"Float32 round-off accumulates through the depth of a network. Each conv, norm, and weight-standardization stage reorders sums in ways that differ from PyTorch's BLAS or cuDNN kernels even when the math is semantically identical, so a 50-layer ResNet against the timm reference reliably lands near 1e-4 absolute diff on order-one activations. The shallower head (one pool plus one Dense) sits closer to 1e-5 because almost no new accumulation happens after the backbone.\n\nThe test gates are LOGITS_ATOL = 1f-3 (absolute, logits) and FEATURES_RTOL = 1f-4 (relative, features). Both are tight enough to catch the silent-divergence failure modes that actually matter (cross-correlation vs convolution, population vs sample variance, GELU approximation mismatch, axis permutations, missing norm epsilon), and loose enough not to fail on legitimate float32 reordering. The features bar is relative because raw pre-norm features on deep / wide backbones (large and xlarge ConvNeXt, huge ConvNeXtV2) drift by ~1e-3 to ~2e-3 absolute even when their downstream logits stay near 1e-5 after the LayerNorm + classifier squashes them; a relative bar keeps the check scale-free across tiny through huge variants. If your port reports a logits max-abs-diff in the 1e-2 range, or a features relative diff in the 1e-2 range or higher, that is a real bug, not round-off; bisect it with the per-stage parity fixtures described below.","title":"Why the bars are ~1e-3 (logits) and ~1e-4 (features), not 1e-5"},{"category":"section","location":"porting/#Reference-example","page":"Porting Backbones","text":"src/Models/ResNetV2/ is the canonical, fully-worked port. Read it first. It exercises most of the shared utilities in one place:\n\nPre-activation residual blocks.\nWeight-standardized convolutions (std_conv from Luximm.Layers).\nGroupNorm with explicit epsilon.\nadapt_input_conv stem adaptation for non-RGB inputs.\nThe full mapping/loader pattern that every other family follows.\n\nIf your port can be expressed as parameter-table changes on top of an existing family, do that. Adding a new variant to BIT_VARIANTS or CONVNEXTV2_VARIANTS is a one-row PR. Adding a new family is the multi-file workflow below.","title":"Reference example"},{"category":"section","location":"porting/#Workflow","page":"Porting Backbones","text":"","title":"Workflow"},{"category":"section","location":"porting/#Phase-1:-Capture-a-parity-fixture","page":"Porting Backbones","text":"Each port starts with an HDF5 fixture produced by a small Python sidecar in test/parity/. The sidecar uses the shared _dump_common.py helpers and follows the dump_<family>_io.py convention. Look at test/parity/dump_resnetv2_bit_io.py for the template.\n\nThe fixture stores /input (deterministic random input, PyTorch NCHW layout), /state_dict/<key> for every PyTorch parameter, and /output/features plus optional /output/logits. The Julia side reads it via Luximm.Interop.read_parity, which reverses the axes so tensors arrive in Lux's WHCN layout.\n\nRun the dump once per variant:\n\nuv run python test/parity/dump_<family>_io.py --variant <timm_name> --out data/parity/<key>_io.h5\n\nOptionally dump a single-channel companion fixture (--in-chans 1, output data/parity/<key>_in1c_io.h5) so the in_chans = 1 parity test can run as well.\n\nFor a brand-new family, capture more than one fixture: the end-to-end one gives a pass/fail signal with no localization power. Add at least three random seeds and per-stage intermediates by registering forward hooks on model.stages[i]. The per-stage fixtures are the bisection tool when the end-to-end test fails.","title":"Phase 1: Capture a parity fixture"},{"category":"section","location":"porting/#Phase-2:-Reuse-the-shared-utilities","page":"Porting Backbones","text":"Do not re-implement anything that already lives under src/Layers/ or src/Interop/. The load-bearing helpers and what they do:\n\nLuximm.Interop.read_parity returns (input, state_dict, output) as Float32 arrays in WHCN layout (the reverse of PyTorch's logical NCHW). Conv weight (out, in, kH, kW) becomes (kW, kH, in, out), which is exactly Lux's Conv layout. For most parameters the layout is what you want and the per-key transform is identity.\napply_state_dict rebuilds the parameter NamedTuple by setting leaves from the dict. Mapping entries are triples (pytorch_key, lux_path_tuple, transform). Non-mutating; bind the result.\nLuximm.Interop.axis_reverse and Luximm.Interop.pyperm are ready-made transforms for the cases where the HDF5-natural layout is not what you want, typically Dense weights and LayerNorm scale/bias.\nstd_conv, layernorm2d, grn_layer are the building blocks that match timm's StdConv2dSame, channel-axis LayerNorm2d, and Global Response Norm.\n\nKeep the per-family weight mapping in src/Models/<Family>/Model.jl as <family>_mapping(state_dict, variant; prefix, num_classes, in_chans), returning a Vector{Tuple{String, Tuple{Vararg{Symbol}}, Function}}. The prefix argument lets a backbone be nested under a wrapper model.","title":"Phase 2: Reuse the shared utilities"},{"category":"section","location":"porting/#Phase-3:-Implement-the-model-with-@compact","page":"Porting Backbones","text":"Lux's @compact is the right primitive for composing layers. The pattern is fixed:\n\n@compact(\n    conv1 = Conv((3, 3), in_ch => out_ch; pad = 1, cross_correlation = true),\n    norm1 = GroupNorm(out_ch, 32; affine = true, epsilon = 1f-5),\n) do x\n    @return NNlib.relu.(norm1(conv1(x)))\nend\n\nNumeric conventions that bite if you forget them, in rough order of how often they cost real debugging time:\n\nCross-correlation, always. PyTorch's Conv2d is cross-correlation; Lux's Conv defaults to true convolution (kernel-flipped). Pass cross_correlation = true to every Conv. Without it, weights load with the right shape but produce mirrored outputs. When you must drop into NNlib.conv directly (weight standardization is the canonical case), pass flipkernel = true on NNlib.DenseConvDims. Same semantic, two flags.\nExplicit padding when zero-padding matters. Conv((k, k), ...; pad = p) works for symmetric same-value padding. For pooling that must pad with zeros instead of -Inf (timm's BiT stem is the canonical case), call NNlib.pad_zeros(x, (l, r, t, b, 0, 0, 0, 0)) first and use pad = 0 on the op.\nNorm defaults are not portable. Always pass epsilon and affine explicitly on GroupNorm, LayerNorm, BatchNorm. PyTorch's nn.GroupNorm uses eps = 1e-5; Lux's default differs. Mismatched epsilons silently shift activations and look like a flaky parity failure.\nVariance corrections. Sample variance (Bessel-corrected, the Julia default) and population variance (corrected = false, what PyTorch uses for BN-style stats and for weight standardization) differ by a factor of N / (N - 1). Pass corrected = false to var whenever you are matching a BN- or WS-style operation. See std_conv in src/Layers/StdConv.jl.\nLux.testmode(st) for parity tests. Otherwise BatchNorm running stats update and any dropout activates, neither of which is what model.eval() does on the PyTorch side.\n\nThe forward should look like math: a sequence of broadcasts and tensor ops with no scalar control flow. No x[i] = ..., no Array(x) inside the forward, no if/else on tensor values, no scalar indexing.","title":"Phase 3: Implement the model with @compact"},{"category":"section","location":"porting/#Phase-4:-Wire-the-variant-table-and-mapping","page":"Porting Backbones","text":"src/Models/<Family>/Config.jl holds the variant catalog:\n\nThe <Family>Variant struct captures the architectural knobs (depths, dims, stem channels) plus hf_repo, default_num_classes, and default_input_size.\n<FAMILY>_VARIANTS :: Dict{Symbol, <Family>Variant} lists every registered variant.\n\nVariant keys are the timm model name with the dot rewritten as an underscore (so the key is a single Julia identifier). The full dot-separated name lives at <FAMILY>_VARIANTS[key].hf_repo.\n\nPick one variant and finish it before adding the second. When a model family has many variants, port the smallest first: it surfaces every shared numeric trap with the fastest test loop, and the mapping function written for one variant typically generalizes by changing only depths or widths. Adding a second variant before the first passes parity creates code paths nothing has exercised and that the first failed test cannot localize.","title":"Phase 4: Wire the variant table and mapping"},{"category":"section","location":"porting/#Phase-5:-Add-the-loader","page":"Porting Backbones","text":"Each family exposes a load_<family>_pretrained(ps, st, variant; num_classes, in_chans, revision, cache_dir, prefix) -> (ps, st) function. All four share this signature: stateless families (BiT/ConvNeXt/ConvNeXtV2) take st and return it unchanged; ResNet mutates it to merge BatchNorm running stats. The flow is identical across families:\n\nLook up the variant in <FAMILY>_VARIANTS.\nValidate num_classes against default_num_classes (or allow 0 for backbone-only).\nCall hf_hub_download to resolve the snapshot path under the HuggingFace Hub cache layout.\nLoad via load_safetensors_state_dict. Pass reverse_axes = true so the safetensors arrays end up in the same WHCN layout the HDF5 fixtures produce; this lets one <family>_mapping function serve both fixture-driven tests and production loading.\nApply via apply_state_dict.\nFor families with BatchNorm running stats (ResNet only), additionally apply the state dict to the model state via <family>_state_mapping and apply_state_dict.\n\nOnce the variant is registered in <FAMILY>_VARIANTS, it is reachable through the family-agnostic create_pretrained and create_model dispatchers automatically — no extra wiring.\n\nKeep the constructor and the weight loading separate. The constructor returns a @compact block; the loader takes the result of Lux.setup and returns a new (ps, st). Mixing the two inside @compact makes the model unusable in tests that want a random init.","title":"Phase 5: Add the loader"},{"category":"section","location":"porting/#Phase-6:-Verify-parity-end-to-end","page":"Porting Backbones","text":"The verification loop is layered. Run the cheap gates between meaningful edits.\n\nEnd-to-end first, against the fixture's state_dict (not the HF download) so the test isolates the forward pass:\n\ndata = Luximm.Interop.read_parity(_FIXTURE_PATH)\nmodel = <family>(variant; in_chans = 3, num_classes = 0)\nps, st = Lux.setup(Xoshiro(0), model)\nst = Lux.testmode(st)\nps = Luximm.Interop.apply_state_dict(ps, data.state_dict,\n                                   <family>_mapping(data.state_dict, variant))\ny, _ = model(data.input, ps, st)\nexpected = data.output[\"features\"]\ndiff = maximum(abs.(y .- expected))\nrel  = diff / max(maximum(abs.(expected)), eps(Float32))\n@test rel < 1f-3\n\nThe convenience wrapper scripts/test_variant.sh runs exactly this for one variant, dumping the fixture first if absent. See Testing for the variant-test workflow.\n\nWhen parity fails, the per-stage and per-block fixtures pay off. Walk the forward by hand: run the partial forward up to each stage and compare against data.output[\"stage_i\"]. The first stage where parity breaks localizes the bug. Inside the failing stage, splice the matching state-dict entries into a single block in isolation and compare against the per-block fixture.","title":"Phase 6: Verify parity end-to-end"},{"category":"section","location":"porting/#Phase-7:-Update-the-bookkeeping-files","page":"Porting Backbones","text":"After parity passes, two repo files need to stay in sync. Forgetting either is a silent regression in CI's path filtering or the user-facing variant table.\n\nREADME.md and the API reference. Update the backbone table at the top of README.md, and confirm the API reference page (docs/src/api/models.md) lists the new constructor and loader.\nci/JimmCI/src/PathFilter.jl. Update _FAMILY_PREFIXES, _FAMILY_EXACT, ALL_FAMILIES, REPRESENTATIVE_VARIANT, and the _SHARED_* lists as appropriate. Without this, CI will silently skip tests for the new code on PR-scope runs; only a full jimm-ci --master sweep would catch it.","title":"Phase 7: Update the bookkeeping files"},{"category":"section","location":"porting/#Pitfalls-reference","page":"Porting Backbones","text":"A running checklist. Every item here has cost real debugging time on a previous port.\n\nCross-correlation. Every Conv needs cross_correlation = true. Every NNlib.conv needs flipkernel = true on its DenseConvDims.\nZero-padded pooling. NNlib.maxpool(pad = 1) pads with -Inf. nn.MaxPool2d(padding = 1) pads with zero. When the input has negative values (post-norm activations, leaky-relu outputs), this diverges. Use pad_zeros first, then pool with pad = 0.\nGN/BN/LN defaults. Always pass epsilon and affine explicitly. Never trust the Lux default to match the PyTorch default.\nVariance correction. Pass corrected = false when matching anything BN-style or WS-style.\nAxis order on apply_state_dict. HDF5 fixtures arrive reversed (Lux-natural). SafeTensors arrive PyTorch-natural by default; pass reverse_axes = true to load_safetensors_state_dict so both sources share one mapping function.\nforward_features vs forward. The fixture and the Julia forward must agree on which one was dumped. Mismatch shows up as a shape error if you are lucky, a silent wrong number if you are not.\nPre-activation block order. norm then activation then conv vs conv then norm then activation. Both compile. Only one matches the upstream architecture.\ntimm's adapt_input_conv for in_chans != 3. The stem weight in the safetensors file is the 3-channel version. The loader applies adapt_input_conv at load time to collapse it. Do not re-collapse on the Julia side.\nLux.testmode(st). Required for parity. Forgetting it makes the test occasionally pass and occasionally fail depending on RNG state inside dropout.\nGeneralize only after one variant passes. Adding a second variant before the first lands creates code paths that the first failed test cannot localize.","title":"Pitfalls reference"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"This page walks through Luximm.jl end-to-end: installing the package, loading a pretrained ResNet50 and predicting an ImageNet class, switching to feature-extractor mode, working with non-RGB inputs, and the HuggingFace cache layout.","title":"Getting Started"},{"category":"section","location":"getting_started/#Installation","page":"Getting Started","text":"Luximm targets Julia 1.12 or newer and is registered in the Julia General registry, so it installs with Pkg.add directly:\n\nusing Pkg\nPkg.add(\"Luximm\")\n\nFor local hacking, clone the repo and Pkg.develop the path:\n\nusing Pkg\nPkg.develop(path = \"/path/to/Luximm.jl\")\n\nThe Python sidecar (pyproject.toml with timm, torch, h5py, safetensors, huggingface-hub) is only needed to regenerate parity fixtures for new backbones. Inference and pretrained-weight loading work from Julia alone. See Testing for the contributor setup.","title":"Installation"},{"category":"section","location":"getting_started/#Loading-ResNet50-and-predicting-an-ImageNet-class","page":"Getting Started","text":"using Luximm, Lux, Random\n\n# `create_pretrained` is family-agnostic; the symbol selects the\n# family. It returns the model and a closure that loads the released\n# weights into a `(ps, st)` pair you produce with `Lux.setup`.\nmodel, load = create_pretrained(:resnet50_a1_in1k)\nps, st = Lux.setup(Xoshiro(0), model)\nps, st = load(ps, st)\nst = Lux.testmode(st)\n\nx = randn(Float32, 224, 224, 3, 1)\nlogits, _ = model(x, ps, st)              # (1000, 1)\n\ntop5 = partialsortperm(vec(logits), 1:5; rev = true)\n\nA few things worth noting in the snippet:\n\nVariant keys are Julia symbols. :resnet50_a1_in1k is the timm model name resnet50.a1_in1k with the dot rewritten as an underscore. The full name with the dot is at RESNET_VARIANTS[:resnet50_a1_in1k].hf_repo.\ncreate_pretrained defaults num_classes to the variant's default_num_classes (1000 for the resnet50.a1_in1k ImageNet checkpoint). Pass an explicit num_classes = 0 for a features-only model, or any other Int for a custom head — see the next two sections.\nLux.setup produces a ps (parameters) and st (state) NamedTuple. The closure returns a new (ps, st) with the HuggingFace weights merged in. Stateless families (BiT, ConvNeXt, ConvNeXtV2, ViT, and the plain VGG variants) return st unchanged; the BatchNorm families (ResNet, SE-ResNet, CoAtNet, and the vgg*_bn variants) merge BatchNorm running statistics into st. Call Lux.testmode(st) before inference so BatchNorm uses those running statistics instead of the current batch's statistics; for the stateless families it is a no-op but still a safe default.\nThe input is shaped (W, H, C, N), Lux's convention. PyTorch's (N, C, H, W) is read-reversed at load time so most weights land in the layout Lux expects directly.\nx here is random noise. For a real prediction, replace it with a preprocessed image: resize to 224x224, scale to Float32 in [0, 1], then normalize with the ImageNet mean and std.\ncreate_model(variant; ...) (without weight loading) is also exported for from-scratch training and for embedding into an outer @compact — see Composing into a larger model below.\n\nTo map the top-5 indices to class names, pair the variant with any ImageNet class label list (Luximm ships none of its own; the timm imagenet_classes.txt works directly because the class index ordering is unchanged).","title":"Loading ResNet50 and predicting an ImageNet class"},{"category":"section","location":"getting_started/#Feature-extractor-mode","page":"Getting Started","text":"Pass num_classes = 0 to drop the classifier head and get the post-stage feature map back instead of logits. This matches timm.create_model(..., num_classes=0).forward_features(x):\n\nmodel, load = create_pretrained(:resnet50_a1_in1k; num_classes = 0)\nps, st = Lux.setup(Xoshiro(0), model)\nps, st = load(ps, st)\nst = Lux.testmode(st)\n\nx = randn(Float32, 224, 224, 3, 1)\nfeatures, _ = model(x, ps, st)            # (7, 7, 2048, 1)\n\nThe output is shaped (W/32, H/32, num_features, N) for every registered backbone. For ResNet50 that is (7, 7, 2048, 1) on a 224x224 input; for convnextv2_atto_fcmae it is (7, 7, 320, 1).\n\nUse this mode when you want to attach Luximm's pretrained encoder to your own downstream head (regression, segmentation, neural ODE, etc.) without carrying around the 1000-class classifier the released checkpoint would otherwise initialize.","title":"Feature extractor mode"},{"category":"section","location":"getting_started/#Single-channel-and-other-non-RGB-inputs","page":"Getting Started","text":"Pass in_chans to create_pretrained. The closure adapts the released 3-channel weight at load time, matching timm's adapt_input_conv semantics: sum across input channels for in_chans = 1, tile and rescale by 3 / in_chans for other counts.\n\nmodel, load = create_pretrained(:convnextv2_atto_fcmae; in_chans = 1)\nps, st = Lux.setup(Xoshiro(0), model)\nps, st = load(ps, st)\nst = Lux.testmode(st)                     # BatchNorm/Dropout in eval mode\n\nx = randn(Float32, 224, 224, 1, 1)\nfeatures, _ = model(x, ps, st)            # (7, 7, 320, 1)\n\nThis is the right path for grayscale medical or scientific imagery, where collapsing the RGB stem is preferable to repeating the single-channel input three times.","title":"Single-channel and other non-RGB inputs"},{"category":"section","location":"getting_started/#Composing-into-a-larger-model","page":"Getting Started","text":"create_pretrained returns the backbone and a closure that already knows the variant; pass prefix = (:backbone,) so the closure writes into the right subtree of the outer Lux.setup result. The backbone itself goes into the outer @compact block as a value:\n\nbackbone, load_backbone = create_pretrained(:resnet50_a1_in1k;\n    num_classes = 0, prefix = (:backbone,))\n\nouter = @compact(\n    backbone = backbone,\n    head     = Dense(2048 => num_outputs),\n) do x\n    head(backbone(x))\nend\n\nps, st = Lux.setup(Xoshiro(0), outer)\nps, st = load_backbone(ps, st)\n\nThe mapping function prefixes every leaf path with prefix..., so apply_state_dict overwrites just the ps.backbone.* and st.backbone.* subtrees. Anything you added (:head, sibling slots, deeper nestings) keeps its Lux.setup initialization.","title":"Composing into a larger model"},{"category":"section","location":"getting_started/#Multiple-backbones-of-the-same-variant","page":"Getting Started","text":"Each create_pretrained call returns its own closure that captures its own prefix, so two backbones of the same variant compose by giving them different prefixes. The two closures are independent and write into disjoint subtrees of ps and st:\n\nbackbone_a, load_a = create_pretrained(:resnet50_a1_in1k;\n    num_classes = 0, prefix = (:resnet50_a,))\nbackbone_b, load_b = create_pretrained(:resnet50_a1_in1k;\n    num_classes = 0, prefix = (:resnet50_b,))\n\nouter = @compact(\n    resnet50_a = backbone_a,\n    resnet50_b = backbone_b,\n    head       = Dense(4096 => num_outputs),\n) do x\n    feats = cat(resnet50_a(x), resnet50_b(x); dims = 3)\n    head(feats)\nend\n\nps, st = Lux.setup(Xoshiro(0), outer)\nps, st = load_a(ps, st)\nps, st = load_b(ps, st)\n\nLoading is opt-in per backbone. Calling load_a and skipping load_b leaves resnet50_b at its Lux.setup random initialization, which is useful when one tower is pretrained and the other is trained from scratch. The prefix tuple must match the slot name used in the outer @compact; otherwise the loader raises leaf path missing key and names the slot it expected to find.","title":"Multiple backbones of the same variant"},{"category":"section","location":"getting_started/#Backbones-nested-deeper-than-one-level","page":"Getting Started","text":"The prefix tuple's length must match the depth of the backbone slot inside the outer model, because the closure prepends prefix... to every Lux leaf path. For a backbone wrapped in an inner @compact that is itself slotted into an outer @compact, the prefix is the sequence of slot names from the outermost block inward:\n\nbackbone, load_backbone = create_pretrained(:resnet50_a1_in1k;\n    num_classes = 0, prefix = (:tower, :backbone))\n\nencoder = @compact(backbone = backbone) do x\n    backbone(x)\nend\n\nouter = @compact(\n    tower = encoder,\n    head  = Dense(2048 => num_outputs),\n) do x\n    head(tower(x))\nend\n\nps, st = Lux.setup(Xoshiro(0), outer)\nps, st = load_backbone(ps, st)\n\nThe weights land in ps.tower.backbone.* because that is where the outer Lux.setup placed the parameter subtree. There is no length limit on the tuple, so the same rule scales to any depth.","title":"Backbones nested deeper than one level"},{"category":"section","location":"getting_started/#Switching-families","page":"Getting Started","text":"create_pretrained (released weights) and create_model (random init) both dispatch on the variant symbol, so picking a different family is just picking a different symbol — no per-family entry points to learn. Each family registers its variants under one <FAMILY>_VARIANTS dict, which is enumerable for variant discovery:\n\nRESNET_VARIANTS. 5 variants.\nBIT_VARIANTS. 15 variants.\nCONVNEXT_VARIANTS. 23 variants (19 Apache 2.0 Facebook AI checkpoints plus 4 Meta DINOv3 encoders).\nCONVNEXTV2_VARIANTS. 26 variants.\n\nSee the API Reference for the full signatures.","title":"Switching families"},{"category":"section","location":"getting_started/#Pretrained-weights-and-the-HuggingFace-cache","page":"Getting Started","text":"The create_pretrained closure resolves model.safetensors against the standard HuggingFace Hub cache layout (HF_HUB_CACHE → $HF_HOME/hub → ~/.cache/huggingface/hub), so the same blob is shared with timm and huggingface_hub: whichever tool downloads first, the other sees a cache hit. Subsequent calls short-circuit on the cached snapshot symlink.\n\nFor gated repos, set HUGGING_FACE_HUB_TOKEN in the environment before calling the loader. The token is forwarded as a bearer header on the resolve and download requests.\n\nFor lower-level access, hf_hub_download returns the resolved snapshot path directly and mirrors the cache semantics of huggingface_hub.hf_hub_download:\n\npath = hf_hub_download(\"timm/resnet50.a1_in1k\",\n                       \"model.safetensors\";\n                       revision = \"main\")\nstate_dict = load_safetensors_state_dict(path)\n\nThis is the escape hatch for cases where you want to inspect or transform the raw PyTorch state dict before applying it.","title":"Pretrained weights and the HuggingFace cache"},{"category":"section","location":"api/#API-Reference","page":"Overview","text":"Luximm.jl exposes three groups of public symbols, all re-exported from the top-level Luximm module.\n\nGroup Purpose Page\nModels The family-agnostic create_model / create_pretrained pair, variant tables, and per-variant config structs. Models\nLayers Building blocks shared across families: weight-standardized conv, channel-axis LayerNorm, Global Response Norm, and timm-equivalent initializers. Layers\nInterop PyTorch and HuggingFace plumbing: HDF5 parity fixtures, state_dict application, axis transforms, HuggingFace Hub cache, SafeTensors loading. Interop\n\nThe top-level Luximm module re-exports every public symbol from these three groups, so end users normally just call using Luximm. The submodule-qualified names (Luximm.Interop.read_parity, Luximm.Layers.std_conv) remain available for cases where the shorter top-level name would collide with something else in the caller's namespace.","title":"API Reference"},{"category":"section","location":"api/#Module-map","page":"Overview","text":"Luximm\n├── Luximm.Models    # family constructors, loaders, variant tables\n├── Luximm.Layers    # std_conv, layernorm2d, grn_layer, init recipes\n└── Luximm.Interop   # parity, HF Hub, SafeTensors\n\nInternal block constructors (resnet_basic_block, convnextv2_block, the stage scaffolding under ConvNeXtCommon, and the per-family resnet / bit_resnetv2 / convnext / convnextv2 constructors themselves) are deliberately unexported. They are implementation details of create_model and create_pretrained; rely on those two as the public construction surface.","title":"Module map"},{"category":"section","location":"api/interop/#Interop","page":"Interop","text":"PyTorch and HuggingFace plumbing: applying a PyTorch state_dict to a Lux (ps, st) pair, resolving and caching weights through the HuggingFace Hub, and loading .safetensors blobs.","title":"Interop"},{"category":"section","location":"api/interop/#State-dict-application","page":"Interop","text":"","title":"State-dict application"},{"category":"section","location":"api/interop/#Weight-layout-transforms","page":"Interop","text":"Per-tensor transforms passed in apply_state_dict mappings to bridge PyTorch's stored layout and the Lux-natural layout (see Porting Backbones).","title":"Weight-layout transforms"},{"category":"section","location":"api/interop/#HuggingFace-Hub","page":"Interop","text":"","title":"HuggingFace Hub"},{"category":"section","location":"api/interop/#SafeTensors","page":"Interop","text":"","title":"SafeTensors"},{"category":"function","location":"api/interop/#Luximm.Interop.apply_state_dict","page":"Interop","text":"apply_state_dict(ps, state_dict, mapping) -> ps\n\nRebuild a Lux parameter NamedTuple by replacing leaves according to mapping, an iterable of (pytorch_key, lux_path, transform) triples where:\n\npytorch_key: dotted name as written by Python's model.state_dict().keys().\nlux_path: tuple of Symbols naming the leaf in ps, e.g. (:stage1, :layer_1, :norm1, :gn, :scale).\ntransform: function Array{Float32} -> Array{Float32} applied to the raw HDF5-read array. Common transforms are identity (HDF5-natural matches Lux) and axis_reverse (full axis reversal, used when the Julia tensor layout matches PyTorch's logical axis order rather than its reversed storage order).\n\nThe original ps is not mutated; the caller must bind the return value.\n\n\n\n\n\n","title":"Luximm.Interop.apply_state_dict"},{"category":"function","location":"api/interop/#Luximm.Interop.read_parity","page":"Interop","text":"read_parity(path) -> NamedTuple\n\nRead a parity HDF5 fixture and return (input, state_dict, output). Tensors are returned as Float32 arrays in their HDF5-natural Julia layout (PyTorch axes reversed). Per-tensor permutations are the caller's responsibility.\n\noutput is a single Array{Float32} if the fixture wrote /output as a dataset, or a Dict{String, Array{Float32}} if it wrote /output/<name>.\n\n\n\n\n\n","title":"Luximm.Interop.read_parity"},{"category":"function","location":"api/interop/#Luximm.Interop.axis_reverse","page":"Interop","text":"axis_reverse(a) -> Array\n\nPermutes all axes in reverse order: (d1, d2, ..., dN) -> (dN, ..., d2, d1). Use this for tensors whose Julia layout was designed in PyTorch axis order. The HDF5 read gives back the reversed layout; this permute restores the original axis order.\n\n\n\n\n\n","title":"Luximm.Interop.axis_reverse"},{"category":"function","location":"api/interop/#Luximm.Interop.pyperm","page":"Interop","text":"pyperm(perm) -> Function\n\nBuild a transform that applies a specific permutation to the HDF5-read array. Useful when the Julia axis order is neither the HDF5-natural reverse nor a full reverse.\n\n\n\n\n\n","title":"Luximm.Interop.pyperm"},{"category":"function","location":"api/interop/#Luximm.Interop.as_channel4d","page":"Interop","text":"as_channel4d(a) -> Array\n\nReshape a (C,) PyTorch norm parameter into (1, 1, C, 1), the shape used by Lux.LayerNorm((1, 1, C); dims = 3) (and so by Luximm.Layers.layernorm2d) for its :scale / :bias leaves on WHCN 4D inputs.\n\n\n\n\n\n","title":"Luximm.Interop.as_channel4d"},{"category":"function","location":"api/interop/#Luximm.Interop.as_token_norm","page":"Interop","text":"as_token_norm(a) -> Array\n\nReshape a (C,) PyTorch norm parameter into (C, 1, 1), the shape used by a channel-axis LayerNorm over a (C, T, N) token tensor (see Luximm.Layers.vit_layernorm) for its :scale / :bias leaves.\n\n\n\n\n\n","title":"Luximm.Interop.as_token_norm"},{"category":"function","location":"api/interop/#Luximm.Interop.adapt_input_conv","page":"Interop","text":"adapt_input_conv(in_chans) -> transform\n\nBuild a state-dict transform that adapts a stem conv weight to the requested input channel count, mirroring timm.models._helpers.adapt_input_conv.\n\nThe transform takes an HDF5-natural Julia conv weight in Lux's (kW, kH, I, O) layout (returned by read_parity or load_safetensors_state_dict) and returns a (kW, kH, in_chans, O) weight following the timm recipe:\n\nin_chans == I: no-op (identity copy as Float32).\nin_chans == 1, I == 3: sum across the input-channel axis (the canonical RGB-to-grayscale collapse).\nin_chans == 1, I > 3, I % 3 == 0: reshape to (kW, kH, 3, I÷3, O) and sum the size-3 axis, leaving (kW, kH, I÷3, O). This branch matches timm's special case for space-to-depth stems.\nin_chans != I, I == 3: tile the weight across the input-channel axis to cover in_chans, truncate, and rescale by 3 / in_chans so the per- output-element response on a uniform input is preserved.\nAny other shape combination raises; timm itself does not support it.\n\nPlug into apply_state_dict mappings in place of identity on the stem weight entry when in_chans != 3.\n\n\n\n\n\n","title":"Luximm.Interop.adapt_input_conv"},{"category":"function","location":"api/interop/#Luximm.Interop.hf_hub_download","page":"Interop","text":"hf_hub_download(repo_id, filename; revision=\"main\",\n                 cache_dir=hf_hub_cache_dir(),\n                 repo_type=\"model\") -> String\n\nResolve <repo_id>/<filename> against revision (a branch, tag, or commit) and return the local snapshot path, downloading only what is not already cached. The on-disk layout matches huggingface_hub:\n\n<cache_dir>/models--<org>--<name>/blobs/<etag>\n<cache_dir>/models--<org>--<name>/snapshots/<commit>/<filename>\n    -> ../../blobs/<etag>\n<cache_dir>/models--<org>--<name>/refs/<revision>     # text: <commit>\n\nThis means a timm.create_model(..., pretrained=True) call and a hf_hub_download call against the same repo see each other's cached blob.\n\nThe function always performs a no-redirect HEAD against https://huggingface.co/<repo_id>/resolve/<revision>/<filename> to look up the current commit (X-Repo-Commit) and the blob's etag (X-Linked-ETag for LFS-backed files, ETag otherwise). If the HEAD fails (e.g. offline), the function falls back to the most recently recorded commit in refs/<revision> and returns the existing snapshot path if present; otherwise the original error is rethrown.\n\nSet repo_type=\"dataset\" for dataset repos; default \"model\" matches timm's usage.\n\n\n\n\n\n","title":"Luximm.Interop.hf_hub_download"},{"category":"function","location":"api/interop/#Luximm.Interop.hf_download","page":"Interop","text":"hf_download(url, dest) -> String\n\nDownload url to dest unless dest already exists. Returns dest.\n\nIf HUGGING_FACE_HUB_TOKEN is set, it is sent as a Bearer token in the Authorization header. The download streams into a sibling temp file and is renamed into place only on success, so an interrupted call (network drop, ^C) never leaves a partial file at dest that a later call would mistake for a cache hit.\n\n\n\n\n\n","title":"Luximm.Interop.hf_download"},{"category":"function","location":"api/interop/#Luximm.Interop.hf_hub_cache_dir","page":"Interop","text":"hf_hub_cache_dir() -> String\n\nCache root that matches huggingface_hub (Python). Honors the same env-var precedence:\n\nHF_HUB_CACHE if set,\notherwise $HF_HOME/hub if HF_HOME is set,\notherwise ~/.cache/huggingface/hub.\n\nFiles downloaded into this directory by Luximm are visible to timm / huggingface_hub, and vice versa.\n\n\n\n\n\n","title":"Luximm.Interop.hf_hub_cache_dir"},{"category":"function","location":"api/interop/#Luximm.Interop.load_safetensors_state_dict","page":"Interop","text":"load_safetensors_state_dict(path; reverse_axes=true) -> Dict{String, Array{Float32}}\n\nRead a .safetensors file from disk into a dict of Float32 arrays.\n\nWhen reverse_axes = true (default), every tensor's axes are reversed so the resulting layout matches read_parity's HDF5-natural Julia layout (PyTorch axes reversed). Set reverse_axes = false to keep PyTorch's logical axis order if a caller wants that layout explicitly.\n\n\n\n\n\n","title":"Luximm.Interop.load_safetensors_state_dict"},{"category":"section","location":"testing/#Testing","page":"Testing","text":"Luximm's correctness story rests on parity tests: for every registered variant, the Julia forward must match timm's forward on the same input and weights. The bar is two-tier:\n\nLogits are checked at an absolute max-abs-diff under TOL = 1f-3. The classifier head is shallow and tightly bounded, so an absolute ceiling is the meaningful end-to-end guarantee.\nFeatures (forward_features and the in_chans=1 companion) are checked at a relative bar, max-abs-diff / max-abs(timm ref) under FEATURES_RTOL = 1f-3. Deep backbones accumulate FP32 rounding through dozens of stages, which inflates raw pre-norm feature diffs by a factor that scales with depth and channel width, even when downstream logits stay tight. A relative bar keeps the check scale-free across tiny through huge variants.\n\nThis page covers the layout of the test suite, how to scope a run to a single variant, how to dump the HDF5 fixture a parity test consumes, and how production CI runs the sweep.","title":"Testing"},{"category":"section","location":"testing/#Test-layout","page":"Testing","text":"Everything lives under test/:\n\ntest/\n├── runtests.jl         # entry point\n├── _filter.jl          # env-var-driven family/variant filtering\n├── parity/             # Python sidecars that produce HDF5 fixtures\n│   ├── _dump_common.py\n│   ├── dump_resnet_io.py\n│   ├── dump_resnetv2_bit_io.py\n│   ├── dump_convnext_io.py\n│   └── dump_convnextv2_io.py\n├── test_resnet.jl      # ResNet parity sweep\n├── test_bit_resnet.jl  # BiT ResNetV2 parity sweep\n├── test_convnext.jl    # ConvNeXt v1 parity sweep\n├── test_convnextv2.jl  # ConvNeXtV2 parity sweep\n├── test_init.jl        # Init-recipe parity (random-init)\n├── test_hf_download.jl # Raw HF download path\n└── test_hf_hub_download.jl  # HF Hub cache layout\n\nruntests.jl consults _filter.jl to decide which family files to include. Each family file iterates over a tuple of variant keys and either runs a parity test against an existing HDF5 fixture or skips the variant if its fixture is missing under data/parity/.\n\nThe three \"infra\" test files (test_init.jl, test_hf_download.jl, test_hf_hub_download.jl) cover the cross-cutting concerns: init recipes, raw HuggingFace downloads, and the Hub cache layout. They run as the infra family.","title":"Test layout"},{"category":"section","location":"testing/#Scoping-a-run-with-environment-variables","page":"Testing","text":"A full parity sweep downloads every released checkpoint and runs a forward through every variant. That is the right thing to do on the CI server, but on a developer machine it is overkill. Two environment variables narrow what runs without editing files:\n\nJIMM_TEST_VARIANTS: comma-separated variant keys (e.g. convnextv2_atto_fcmae). Setting this alone also restricts the active families to whichever ones contain the listed variants and drops the infra family, so a single variant key is enough to scope a run.\nJIMM_TEST_FAMILIES: comma-separated list of families. Recognized values are infra, bit, resnet, convnext, convnextv2. When set, this is authoritative and overrides the family inference from JIMM_TEST_VARIANTS. Unset and JIMM_TEST_VARIANTS also unset means every family runs.\n\n# Just the ConvNeXtV2 atto fcmae parity tests, nothing else:\nJIMM_TEST_VARIANTS=convnextv2_atto_fcmae \\\n    julia --project -e 'using Pkg; Pkg.test()'\n\n# Include the infra checks alongside a single backbone variant:\nJIMM_TEST_FAMILIES=infra,convnextv2 \\\nJIMM_TEST_VARIANTS=convnextv2_atto_fcmae \\\n    julia --project -e 'using Pkg; Pkg.test()'\n\nParity tests also skip when their HDF5 fixture is missing under data/parity/, so a contributor can dump one variant's fixture (and optionally its _in1c companion) and run just that one without touching the test code.","title":"Scoping a run with environment variables"},{"category":"section","location":"testing/#Parity-fixtures","page":"Testing","text":"A parity fixture is an HDF5 file produced by one of the Python sidecars under test/parity/. It contains:\n\n/input: deterministic torch.randn input in PyTorch NCHW layout.\n/state_dict/<key>: every PyTorch parameter, keyed by its state_dict name.\n/output/features: the result of model.forward_features(input).\n/output/logits (when the variant ships a trained head): the result of model.forward(input).\n\nFixtures live in data/parity/ and follow the naming convention <variant_key>_io.h5 (and <variant_key>_in1c_io.h5 for single-channel variants). The directory is gitignored; fixtures are not redistributed.\n\nThe Julia side consumes fixtures via Luximm.Interop.read_parity, which returns a NamedTuple (input, state_dict, output) with all arrays already axis-reversed from PyTorch NCHW into Lux's WHCN layout. The mapping function for the family then routes each state-dict key into the corresponding Lux parameter path via apply_state_dict.","title":"Parity fixtures"},{"category":"section","location":"testing/#Dumping-a-fixture","page":"Testing","text":"uv run python test/parity/dump_<family>_io.py \\\n    --variant <timm_name> \\\n    --out data/parity/<variant_key>_io.h5\n\n<timm_name> is the dot-separated timm model name (e.g. convnextv2_atto.fcmae). <variant_key> is the Julia symbol form with the dot rewritten as an underscore. Pass --in-chans 1 to produce the single-channel companion fixture; the output filename suffix changes from _io to _in1c_io.\n\nThe first dump for a family materializes the Python sidecar environment (PyTorch plus timm plus the small HDF5 helpers). uv sync against the repo's pyproject.toml is the supported provisioning path.","title":"Dumping a fixture"},{"category":"section","location":"testing/#The-scripts/test_variant.sh-wrapper","page":"Testing","text":"For the common case (one variant, dump-if-missing then test), scripts/test_variant.sh chains the fixture dump and the Julia invocation:\n\n# Resolve family, dump fixture if absent, run only this variant:\nscripts/test_variant.sh convnextv2_atto_fcmae\n\n# Classic ResNet18:\nscripts/test_variant.sh resnet18_a1_in1k\n\n# Single-channel parity test (dumps the _in1c fixture):\nscripts/test_variant.sh convnextv2_atto_fcmae --in-chans 1\n\n# Force a fresh fixture dump even if one already exists:\nscripts/test_variant.sh convnextv2_atto_fcmae --force\n\nThe script resolves the family from the variant prefix, calls the appropriate Python sidecar under test/parity/ via uv run if the HDF5 fixture is missing, then runs the Julia test suite with JIMM_TEST_VARIANTS=<variant> set. Requires both uv and julia on PATH.","title":"The scripts/test_variant.sh wrapper"},{"category":"section","location":"testing/#CI","page":"Testing","text":"Production CI runs on a self-hosted Linux VM via a Julia TUI driver named jimm-ci (under ci/JimmCI/). The setup is unusual on purpose: the parity test suite needs hundreds of gigabytes of timm reference weights and per-variant HDF5 fixtures that each take a few minutes of CPU plus a PyTorch environment to produce. Hosted CI runners would re-download and re-generate that material on every run; a self-hosted machine with a persistent state directory hits the cache instead.\n\nSee ci/README.md for the full deployment story (App registration, secrets layout, the TUI keybindings, and how PR runs map paths to test families). The short version, from a contributor's perspective:\n\nPRs are reviewed by a maintainer and approved by selecting their row in the jimm-ci TUI on the VM. Fork PRs are filtered out client-side and never produce a Check Run.\nThe runner consults ci/JimmCI/src/PathFilter.jl to map changed paths to families. If you add a new family or rename a shared module, that file must be updated; otherwise CI silently skips tests for the new code.\nPer-PR Check Runs come back through the standard GitHub Checks API. Look at the jimm-ci / <family> checks on the PR.\n\nThe separate documentation workflow under .github/workflows/docs.yml is a hosted GitHub Actions job. It builds and deploys this documentation site without needing parity weights or fixtures, so the regular runners are sufficient for that path.","title":"CI"},{"category":"section","location":"api/layers/#Layers","page":"Layers","text":"Reusable building blocks shared across model families. These exist to match specific timm constructs in semantics, layout, and default parameters. They are not intended as a general-purpose layer library.","title":"Layers"},{"category":"section","location":"api/layers/#Building-blocks","page":"Layers","text":"","title":"Building blocks"},{"category":"section","location":"api/layers/#Helpers","page":"Layers","text":"","title":"Helpers"},{"category":"section","location":"api/layers/#Initializers","page":"Layers","text":"","title":"Initializers"},{"category":"function","location":"api/layers/#Luximm.Layers.std_conv","page":"Layers","text":"std_conv(kW, kH, in, out; stride=1, pad=0, eps=1f-8) -> @compact block\n\nCross-correlation convolution whose kernel is standardized at forward time. Use this in place of Conv when porting a timm model that wraps its convolutions in StdConv2d (BiT-ResNet, NFNet, etc.).\n\n\n\n\n\n","title":"Luximm.Layers.std_conv"},{"category":"function","location":"api/layers/#Luximm.Layers.layernorm2d","page":"Layers","text":"layernorm2d(C; eps=1f-6) -> Lux.LayerNorm\n\nLayerNorm with per-channel affine parameters for a 4D tensor in Lux's (W, H, C, N) layout. Normalizes over the channel axis at each spatial location and batch element, then applies a learnable scale and bias. Numerically equivalent to timm's LayerNorm2d.\n\nImplemented as Lux.LayerNorm((1, 1, C); dims = 3, epsilon = eps), so the affine parameter leaves :scale and :bias have shape (1, 1, C, 1). PyTorch state-dict entries <prefix>.weight and <prefix>.bias are stored as (C,) and must be reshaped to (1, 1, C, 1) when loading (see Luximm.Interop.as_channel4d).\n\n\n\n\n\n","title":"Luximm.Layers.layernorm2d"},{"category":"function","location":"api/layers/#Luximm.Layers.grn_layer","page":"Layers","text":"grn_layer(C; eps=1f-6) -> @compact block\n\nGlobal Response Normalization for (W, H, C, N) tensors. Computes the L2 norm of each channel's spatial map, normalizes that by the channel-mean of those norms, and applies a per-channel affine x + bias + scale * (x * n) with both parameters initialized to zero.\n\nNamed grn_layer to keep the @compact field name :grn available for use in containing blocks, so PyTorch keys like mlp.grn.weight map directly to (..., :grn, :scale).\n\nPyTorch state-dict keys <prefix>.weight and <prefix>.bias map to the :scale and :bias leaves of this layer with the identity transform.\n\n\n\n\n\n","title":"Luximm.Layers.grn_layer"},{"category":"function","location":"api/layers/#Luximm.Layers.se_block","page":"Layers","text":"se_block(C; rd_ratio=1/16, rd_divisor=8, rd_channels=nothing, act=NNlib.relu) -> @compact block\n\nSqueeze-and-excitation channel-attention block for (W, H, C, N) tensors. Global-average-pools each channel to a scalar, runs a two-layer 1x1-conv bottleneck (fc1 C→rd → act → fc2 rd→C), and rescales the input by the per-channel sigmoid gate. The bottleneck width rd is rd_channels when given (e.g. CoAtNet's int(attn_ratio * mid_chs)), otherwise se_make_divisible(C * rd_ratio, rd_divisor), matching timm's SEModule. act is the bottleneck activation (ReLU by default; CoAtNet's later recipes use SiLU).\n\nPyTorch keys <prefix>.fc1.weight/bias and <prefix>.fc2.weight/bias map to the :fc1 / :fc2 Conv leaves with the identity transform.\n\n\n\n\n\n","title":"Luximm.Layers.se_block"},{"category":"function","location":"api/layers/#Luximm.Layers.patch_embed","page":"Layers","text":"patch_embed(in_chans, embed_dim; patch=16) -> @compact block\n\nSplit a (W, H, in_chans, N) image into patch x patch patches via a stride-patch conv and return a token tensor (embed_dim, num_tokens, N) where num_tokens = (W/patch) * (H/patch) and tokens run width-fastest, matching timm's PatchEmbed.\n\nPyTorch keys <prefix>.proj.weight / <prefix>.proj.bias map to the :proj Conv leaves (identity, or adapt_input_conv for the weight when in_chans != 3).\n\n\n\n\n\n","title":"Luximm.Layers.patch_embed"},{"category":"function","location":"api/layers/#Luximm.Layers.mhsa","page":"Layers","text":"mhsa(dim; num_heads, qkv_bias=true) -> @compact block\n\nMulti-head self-attention over a (dim, T, N) token tensor. Splits a fused qkv projection into num_heads heads of width dim ÷ num_heads, computes scaled dot-product attention (scale 1/sqrt(head_dim), softmax over the key axis), merges heads, and applies the output proj. Numerically matches timm's Attention with qk_norm=False.\n\nPyTorch keys map as: attn.qkv.weight → (:qkv, :weight) (axis_reverse), attn.qkv.bias → (:qkv, :bias) (identity), attn.proj.weight → (:proj, :weight) (axis_reverse), attn.proj.bias → (:proj, :bias) (identity).\n\n\n\n\n\n","title":"Luximm.Layers.mhsa"},{"category":"function","location":"api/layers/#Luximm.Layers.vit_block","page":"Layers","text":"vit_block(dim; num_heads, mlp_ratio=4, eps=1f-6) -> @compact block\n\nA single pre-norm ViT encoder block over a (dim, T, N) token tensor: LayerNorm → mhsa → residual, then LayerNorm → MLP (fc1 → exact GELU → fc2) → residual.\n\nPyTorch keys map as: norm1/norm2.{weight,bias} → (:norm1/:norm2, :scale/:bias) (as_token_norm), attn.* via mhsa, mlp.fc1/fc2.* → (:fc1/:fc2, :weight/:bias) (axis_reverse/identity).\n\n\n\n\n\n","title":"Luximm.Layers.vit_block"},{"category":"function","location":"api/layers/#Luximm.Layers.vit_layernorm","page":"Layers","text":"vit_layernorm(dim; eps=1f-6) -> Lux.LayerNorm\n\nLayerNorm over the channel axis (axis 1) of a (dim, T, N) token tensor, with per-channel affine. Matches timm's ViT nn.LayerNorm(dim, eps=1e-6). The affine :scale / :bias leaves have shape (dim, 1, 1), so the PyTorch (dim,) parameter is reshaped with Luximm.Interop.as_token_norm when loading.\n\n\n\n\n\n","title":"Luximm.Layers.vit_layernorm"},{"category":"function","location":"api/layers/#Luximm.Layers.rel_pos_attention","page":"Layers","text":"rel_pos_attention(dim, dim_out; dim_head=32, window) -> @compact block\n\n2D relative-position-bias multi-head self-attention over a (dim, ...) WHCN feature map. qkv is a fused 1x1 conv dim => 3*dim; proj is a 1x1 conv dim => dim_out. Heads number dim ÷ dim_head. A learned bias from rel_pos_bias_table (gathered by a precomputed relative-position index for the window = (Wh, Ww) grid) is added to the attention scores before the key-axis softmax. Numerically matches timm's Attention2d(head_first=True, expand_first=False) with RelPosBias.\n\nPyTorch keys: qkv.weight/bias → (:qkv, :weight/:bias) (identity/identity, 1x1 conv), proj.weight/bias → (:proj, :weight/:bias), rel_pos.relative_position_bias_table → (:rel_pos_bias_table,) (axis_reverse, to (num_rel, num_heads)).\n\n\n\n\n\n","title":"Luximm.Layers.rel_pos_attention"},{"category":"function","location":"api/layers/#Luximm.Layers.se_make_divisible","page":"Layers","text":"se_make_divisible(v, divisor=8; min_value=divisor) -> Int\n\ntimm's make_divisible: round v to the nearest multiple of divisor, never dropping below min_value and never losing more than 10% of v. Used to size the SE bottleneck channel count.\n\n\n\n\n\n","title":"Luximm.Layers.se_make_divisible"},{"category":"function","location":"api/layers/#Luximm.Layers.rel_pos_index","page":"Layers","text":"rel_pos_index(Wh, Ww) -> Matrix{Int}\n\nRelative-position index grid for an Wh x Ww window, matching timm's gen_relative_position_index. Returns a (L, L) matrix (L = Wh*Ww, tokens in width-fastest order) of 1-based row indices into a (2*Wh-1)*(2*Ww-1)-row bias table. Entry [i, j] is the bias-table row for the relative offset between query token i and key token j.\n\n\n\n\n\n","title":"Luximm.Layers.rel_pos_index"},{"category":"function","location":"api/layers/#Luximm.Layers.kaiming_normal_fan_out","page":"Layers","text":"kaiming_normal_fan_out(rng, [T,] dims...) -> Array{T}\n\nPyTorch's nn.init.kaiming_normal_(weight, mode='fan_out', nonlinearity='relu').\n\nstd = sqrt(2 / fan_out) where fan_out follows PyTorch's _calculate_fan_in_and_fan_out: out_channels * prod(receptive_field). Lux Conv weight shape is (kW, kH, in, out), so fan_out = dims[end] * prod(dims[1:end-2]). For Dense weight shape (out, in), fan_out = dims[1].\n\n\n\n\n\n","title":"Luximm.Layers.kaiming_normal_fan_out"},{"category":"function","location":"api/layers/#Luximm.Layers.normal_init","page":"Layers","text":"normal_init(; std) -> (rng, dims...) -> Array{Float32}\n\nClosure that produces Normal(0, std) samples in Float32, mirroring PyTorch's nn.init.normal_(weight, mean=0., std=std). Used for timm's BiT classifier head (std = 0.01).\n\n\n\n\n\n","title":"Luximm.Layers.normal_init"},{"category":"section","location":"#Luximm.jl","page":"Home","text":"Julia ports of timm (PyTorch Image Models, by Ross Wightman) backbones for Lux.jl, with pretrained weights loaded directly from HuggingFace Hub in .safetensors format. The name is an homage to the project we port from.","title":"Luximm.jl"},{"category":"section","location":"#Status","page":"Home","text":"Most of Luximm was written by AI agents driving the porting workflow encoded in .claude/skills/timm-to-lux/, with human review at each phase and the parity tests as the correctness backstop. The code is already being used in real projects, so the registered backbones work for forward inference with the released weights. That said: expect bugs and rough edges, especially around anything the parity tests do not exercise (custom training loops, mixed-precision paths, exotic input shapes). File issues and PRs.","title":"Status"},{"category":"section","location":"#Available-backbones","page":"Home","text":"Family Variant prefix Weights Weight License Commercial Use\nResNet :resnet* 5 Apache 2.0 ✅\nBiT ResNetV2 :resnetv2_*_bit_* 15 Apache 2.0 ✅\nConvNeXt :convnext_* 19 Apache 2.0 ✅\nConvNeXt (DINOv3) :convnext_* 4 DINOv3 License ⚠️\nConvNeXt V2 :convnextv2_* 26 CC BY-NC 4.0 ❌\nVGG :vgg* 8 BSD-3-Clause ✅\nSE-ResNet :seresnet* 1 Apache 2.0 ✅\nViT :vit_* 1 Apache 2.0 ✅\nCoAtNet :coatnet_* 5 Apache 2.0 ✅","title":"Available backbones"},{"category":"section","location":"#At-a-glance","page":"Home","text":"using Luximm, Lux, Random\n\n# ResNet50 with the trained 1000-class ImageNet head.\n# `create_pretrained` is family-agnostic; the symbol selects the\n# family. It returns the model and a closure that loads the released\n# weights into `(ps, st)` once you've run `Lux.setup`.\nmodel, load = create_pretrained(:resnet50_a1_in1k)\nps, st = Lux.setup(Xoshiro(0), model)\nps, st = load(ps, st)\nst = Lux.testmode(st)                 # BatchNorm/Dropout in eval mode\n\nx = randn(Float32, 224, 224, 3, 1)\nlogits, _ = model(x, ps, st)          # (1000, 1)\ntop1 = argmax(vec(logits))            # ImageNet class index\n\nSee Getting Started for the full walkthrough, including feature-extractor mode (num_classes = 0) and single-channel inputs.","title":"At a glance"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Getting Started: end-to-end prediction example, switching families, grayscale inputs, HuggingFace cache.\nPorting Backbones: contributor guide for adding a new timm backbone, with the parity-driven workflow.\nTesting: how the parity test suite is structured, the env-var filters, and how to dump a fixture for a new variant.\nAPI Reference: every exported function and type.","title":"Where to go next"},{"category":"section","location":"api/models/#Models","page":"Models","text":"","title":"Models"},{"category":"section","location":"api/models/#Family-agnostic-interface","page":"Models","text":"create_pretrained is the symbol-dispatched entry point for loading released weights. It returns the model and a closure that loads the HuggingFace checkpoint into a (ps, st) pair you produce with Lux.setup:\n\nmodel, load = create_pretrained(variant)\nps, st = Lux.setup(rng, model)\nps, st = load(ps, st)\n\nThe closure captures variant, in_chans, num_classes, and the HF / prefix kwargs at construction time, so the loader body no longer needs to introspect ps to recover what you already told it. create_model is the random-init counterpart: it returns the bare @compact model with no weights loaded. See Getting Started for the nested pattern with prefix.","title":"Family-agnostic interface"},{"category":"section","location":"api/models/#Per-family-namespaces","page":"Models","text":"Each family exports its variant config struct and the <FAMILY>_VARIANTS registry dict. The remaining family internals (per-family constructors, weight mappings, state mappings) live in Luximm.Models.* for callers who need to escape the create_pretrained / create_model front door.","title":"Per-family namespaces"},{"category":"section","location":"api/models/#ResNet","page":"Models","text":"","title":"ResNet"},{"category":"section","location":"api/models/#Registered-variants","page":"Models","text":"using Markdown, Luximm\nrows = sort(collect(Luximm.RESNET_VARIANTS); by = p -> String(first(p)))\nio = IOBuffer()\nprintln(io, \"| Variant | num_classes | num_features | input size |\")\nprintln(io, \"|:---|---:|---:|---:|\")\nfor (k, v) in rows\n    println(io, \"| [`:$(k)`](https://huggingface.co/$(v.hf_repo)) | $(v.default_num_classes) | $(v.num_features) | $(v.default_input_size) |\")\nend\nMarkdown.parse(String(take!(io)))","title":"Registered variants"},{"category":"section","location":"api/models/#SE-ResNet","page":"Models","text":"","title":"SE-ResNet"},{"category":"section","location":"api/models/#Registered-variants-2","page":"Models","text":"using Markdown, Luximm\nrows = sort(collect(Luximm.SERESNET_VARIANTS); by = p -> String(first(p)))\nio = IOBuffer()\nprintln(io, \"| Variant | num_classes | num_features | input size |\")\nprintln(io, \"|:---|---:|---:|---:|\")\nfor (k, v) in rows\n    println(io, \"| [`:$(k)`](https://huggingface.co/$(v.hf_repo)) | $(v.default_num_classes) | $(v.num_features) | $(v.default_input_size) |\")\nend\nMarkdown.parse(String(take!(io)))","title":"Registered variants"},{"category":"section","location":"api/models/#BiT-ResNetV2","page":"Models","text":"","title":"BiT ResNetV2"},{"category":"section","location":"api/models/#Registered-variants-3","page":"Models","text":"using Markdown, Luximm\nrows = sort(collect(Luximm.BIT_VARIANTS); by = p -> String(first(p)))\nio = IOBuffer()\nprintln(io, \"| Variant | num_classes | num_features | input size |\")\nprintln(io, \"|:---|---:|---:|---:|\")\nfor (k, v) in rows\n    println(io, \"| [`:$(k)`](https://huggingface.co/$(v.hf_repo)) | $(v.default_num_classes) | $(v.num_features) | $(v.default_input_size) |\")\nend\nMarkdown.parse(String(take!(io)))","title":"Registered variants"},{"category":"section","location":"api/models/#ConvNeXt","page":"Models","text":"warning: DINOv3 weights are not Apache 2.0\nThe four :convnext_*_dinov3_lvd1689m encoders are released by Meta under the DINOv3 License, which imposes obligations on outputs derived from the weights that differ from a standard permissive open-source license. Read the license before using the weights for any downstream task. This applies only to the weights; the Julia code in this package is Apache 2.0. The Facebook AI .fb_* checkpoints carry the upstream Apache 2.0 license and are unaffected.","title":"ConvNeXt"},{"category":"section","location":"api/models/#Registered-variants-4","page":"Models","text":"using Markdown, Luximm\nrows = sort(collect(Luximm.CONVNEXT_VARIANTS); by = p -> String(first(p)))\nio = IOBuffer()\nprintln(io, \"| Variant | num_classes | num_features | input size |\")\nprintln(io, \"|:---|---:|---:|---:|\")\nfor (k, v) in rows\n    println(io, \"| [`:$(k)`](https://huggingface.co/$(v.hf_repo)) | $(v.default_num_classes) | $(v.dims[end]) | $(v.default_input_size) |\")\nend\nMarkdown.parse(String(take!(io)))","title":"Registered variants"},{"category":"section","location":"api/models/#ConvNeXt-V2","page":"Models","text":"warning: ConvNeXtV2 weights are non-commercial\nEvery ConvNeXtV2 checkpoint is released by Meta under Creative Commons Attribution-NonCommercial 4.0. Commercial use of these weights is not permitted. This applies to every row in the variant table below and is independent of Luximm.jl's own Apache 2.0 code license. If commercial use matters, BiT (Apache 2.0) or the ConvNeXt v1 .fb_* checkpoints (Apache 2.0) are the alternatives.","title":"ConvNeXt V2"},{"category":"section","location":"api/models/#Registered-variants-5","page":"Models","text":"using Markdown, Luximm\nrows = sort(collect(Luximm.CONVNEXTV2_VARIANTS); by = p -> String(first(p)))\nio = IOBuffer()\nprintln(io, \"| Variant | num_classes | num_features | input size |\")\nprintln(io, \"|:---|---:|---:|---:|\")\nfor (k, v) in rows\n    println(io, \"| [`:$(k)`](https://huggingface.co/$(v.hf_repo)) | $(v.default_num_classes) | $(v.dims[end]) | $(v.default_input_size) |\")\nend\nMarkdown.parse(String(take!(io)))","title":"Registered variants"},{"category":"section","location":"api/models/#VGG","page":"Models","text":"","title":"VGG"},{"category":"section","location":"api/models/#Registered-variants-6","page":"Models","text":"using Markdown, Luximm\nrows = sort(collect(Luximm.VGG_VARIANTS); by = p -> String(first(p)))\nio = IOBuffer()\nprintln(io, \"| Variant | num_classes | num_features | input size |\")\nprintln(io, \"|:---|---:|---:|---:|\")\nfor (k, v) in rows\n    println(io, \"| [`:$(k)`](https://huggingface.co/$(v.hf_repo)) | $(v.default_num_classes) | 512 | $(v.default_input_size) |\")\nend\nMarkdown.parse(String(take!(io)))","title":"Registered variants"},{"category":"section","location":"api/models/#ViT","page":"Models","text":"","title":"ViT"},{"category":"section","location":"api/models/#Registered-variants-7","page":"Models","text":"using Markdown, Luximm\nrows = sort(collect(Luximm.VIT_VARIANTS); by = p -> String(first(p)))\nio = IOBuffer()\nprintln(io, \"| Variant | num_classes | num_features | input size |\")\nprintln(io, \"|:---|---:|---:|---:|\")\nfor (k, v) in rows\n    println(io, \"| [`:$(k)`](https://huggingface.co/$(v.hf_repo)) | $(v.default_num_classes) | $(v.embed_dim) | $(v.default_input_size) |\")\nend\nMarkdown.parse(String(take!(io)))","title":"Registered variants"},{"category":"section","location":"api/models/#CoAtNet","page":"Models","text":"","title":"CoAtNet"},{"category":"section","location":"api/models/#Registered-variants-8","page":"Models","text":"using Markdown, Luximm\nrows = sort(collect(Luximm.COATNET_VARIANTS); by = p -> String(first(p)))\nio = IOBuffer()\nprintln(io, \"| Variant | num_classes | num_features | input size |\")\nprintln(io, \"|:---|---:|---:|---:|\")\nfor (k, v) in rows\n    println(io, \"| [`:$(k)`](https://huggingface.co/$(v.hf_repo)) | $(v.default_num_classes) | $(v.dims[end]) | $(v.default_input_size) |\")\nend\nMarkdown.parse(String(take!(io)))","title":"Registered variants"},{"category":"function","location":"api/models/#Luximm.Models.create_pretrained","page":"Models","text":"create_pretrained(variant; in_chans=3, num_classes=nothing,\n                  revision=\"main\", cache_dir=hf_hub_cache_dir(),\n                  prefix=()) -> (model, load)\n\nFamily-agnostic pretrained-weight entry point, mirroring timm.create_model(..., pretrained=True). Returns the model and a closure that loads the released model.safetensors into a (ps, st) pair the caller produced with Lux.setup. The closure captures variant, in_chans, num_classes, and the HF / prefix kwargs at construction time, so calling it is the only place (ps, st) need to be threaded.\n\nmodel, load = create_pretrained(:resnet50_a1_in1k)\nps, st = Lux.setup(Xoshiro(0), model)\nps, st = load(ps, st)\n\nnum_classes = nothing (the default) builds the head the released checkpoint ships with — default_num_classes(variant). Pass an explicit 0 for a features-only model, or any other Int to swap in a custom-width head (the released classifier is then skipped and the warning case fires).\n\nFor composition, build model separately and pass it into an outer @compact, capturing prefix = (:backbone,) so the closure writes into the right subtree:\n\nbackbone, load_backbone = create_pretrained(:resnet50_a1_in1k;\n    num_classes = 0, prefix = (:backbone,))\nouter = @compact(backbone = backbone,\n    head = Dense(2048 => num_outputs)) do x\n    head(backbone(x))\nend\nps, st = Lux.setup(rng, outer)\nps, st = load_backbone(ps, st)\n\n\n\n\n\n","title":"Luximm.Models.create_pretrained"},{"category":"function","location":"api/models/#Luximm.Models.create_model","page":"Models","text":"create_model(variant; kwargs...) -> model\n\nFamily-agnostic random-init model constructor, mirroring timm.create_model(..., pretrained=False). Dispatches on variant to the matching family constructor and returns the bare @compact model — no parameters, no state, no pretrained weights.\n\nUse this when you want to train from scratch, or as a building block inside an outer @compact when composing a larger model. To load the released weights for a variant, use create_pretrained instead.\n\nmodel = create_model(:resnet50_a1_in1k; num_classes = 1000)\nps, st = Lux.setup(rng, model)        # random init, ready for training\n\nkwargs are forwarded to the family constructor (in_chans, num_classes).\n\n\n\n\n\n","title":"Luximm.Models.create_model"},{"category":"function","location":"api/models/#Luximm.Models.default_num_classes","page":"Models","text":"default_num_classes(variant) -> Int\n\nHead dimension the released checkpoint for variant was trained at. Returns 0 for encoder-only variants (DINOv3 ConvNeXt, ConvNeXtV2 fcmae pretrains).\n\n\n\n\n\n","title":"Luximm.Models.default_num_classes"},{"category":"type","location":"api/models/#Luximm.Models.ResNetVariant","page":"Models","text":"ResNetVariant\n\nArchitectural config for a classic timm ResNet variant.\n\nFields:\n\nname: lookup key (e.g. :resnet50_a1_in1k).\nblock: residual block type, either :basic (used by r18/r34) or :bottleneck (used by r50/r101/r152).\nlayers: per-stage block count (d1, d2, d3, d4).\nplanes: base channel widths per stage (64, 128, 256, 512). Multiplied by 4 inside :bottleneck stages to give the actual output channel count.\nnum_features: backbone output channels (planes[end] for :basic, planes[end] * 4 for :bottleneck).\nhf_repo: HuggingFace repo containing model.safetensors.\ndefault_num_classes: head dimension the released weights ship with.\ndefault_input_size: native training resolution (224 for every registered variant). Informational only: the model is fully convolutional and accepts any size.\n\n\n\n\n\n","title":"Luximm.Models.ResNetVariant"},{"category":"constant","location":"api/models/#Luximm.Models.RESNET_VARIANTS","page":"Models","text":"RESNET_VARIANTS :: Dict{Symbol, ResNetVariant}\n\nLookup table for classic ResNet variants currently ported from timm. Keys are the timm model names with dots rewritten as underscores.\n\n\n\n\n\n","title":"Luximm.Models.RESNET_VARIANTS"},{"category":"type","location":"api/models/#Luximm.Models.SEResNetVariant","page":"Models","text":"SEResNetVariant\n\nArchitectural config for a single SE-ResNet variant.\n\nFields:\n\nname: lookup key (e.g. :seresnet50_a1_in1k).\nlayers: per-stage block count (d1, d2, d3, d4).\nplanes: base channel widths per stage (64, 128, 256, 512). Multiplied by 4 (the bottleneck expansion) to give the actual stage output channels.\nnum_features: backbone output channels (planes[end] * 4 = 2048).\nhf_repo: HuggingFace repo containing model.safetensors.\ndefault_num_classes: head dimension the released weights ship with.\ndefault_input_size: native training resolution (224). Informational only.\nse_reduction: SE bottleneck reduction divisor (16 for every variant); the SE inner width is se_make_divisible(out_ch / se_reduction, 8).\n\n\n\n\n\n","title":"Luximm.Models.SEResNetVariant"},{"category":"constant","location":"api/models/#Luximm.Models.SERESNET_VARIANTS","page":"Models","text":"SERESNET_VARIANTS :: Dict{Symbol, SEResNetVariant}\n\nLookup table for the SE-ResNet variants ported from timm. Keys are the timm model name with dots rewritten as underscores.\n\n\n\n\n\n","title":"Luximm.Models.SERESNET_VARIANTS"},{"category":"type","location":"api/models/#Luximm.Models.BiTVariant","page":"Models","text":"BiTVariant\n\nArchitectural config for a single BiT ResNetV2 variant.\n\nFields:\n\nname: lookup key (e.g. :resnetv2_50x1_bit_goog_in21k).\nlayers: per-stage depth tuple (3,4,6,3) for r50, (3,4,23,3) for r101, (3,8,36,3) for r152.\nwidth_factor: integer width multiplier from the timm name suffix (x1, x2, x3, x4).\nstem_chs: stem output channels (64 * width_factor).\nstage_chs: per-stage output channel tuple (base widths (256,512,1024,2048) scaled by width_factor).\nnum_features: backbone output channels (stage_chs[end]).\nhf_repo: HuggingFace repo containing model.safetensors.\ndefault_num_classes: head dimension the released weights were trained with (21843 for goog_in21k, 1000 for the in1k tags).\ndefault_input_size: native training resolution (224 for most tags, 384 for the _384 teacher variant). The model itself is fully convolutional and accepts any input size; this is just what the released weights were tuned at.\n\n\n\n\n\n","title":"Luximm.Models.BiTVariant"},{"category":"constant","location":"api/models/#Luximm.Models.BIT_VARIANTS","page":"Models","text":"BIT_VARIANTS :: Dict{Symbol, BiTVariant}\n\nLookup table for the BiT variants this package currently ports. Keys mirror the timm model name with the dot rewritten as an underscore (the dot is reserved in Julia identifiers); the full timm name with the dot lives at BIT_VARIANTS[key].hf_repo.\n\n\n\n\n\n","title":"Luximm.Models.BIT_VARIANTS"},{"category":"type","location":"api/models/#Luximm.Models.ConvNeXtVariant","page":"Models","text":"ConvNeXtVariant\n\nArchitectural config for a single ConvNeXt v1 variant.\n\nFields:\n\nname: lookup key (e.g. :convnext_tiny_dinov3_lvd1689m).\ndepths: per-stage block count, (d1, d2, d3, d4).\ndims: per-stage channel widths, (c1, c2, c3, c4). c1 is also the stem output channels. c4 is num_features.\nhf_repo: HuggingFace repo containing model.safetensors.\ndefault_num_classes: head dimension the released weights ship with. 0 for the DINO encoders (no usable head).\ndefault_input_size: native training resolution (224, 384, …) for the released checkpoint. Informational only: the model is fully convolutional and accepts any size, so this is not enforced.\nls_init: LayerScale init value (gamma parameter in timm). All v1 variants released so far use 1e-6; kept as a field in case future ports need a different value.\n\n\n\n\n\n","title":"Luximm.Models.ConvNeXtVariant"},{"category":"constant","location":"api/models/#Luximm.Models.CONVNEXT_VARIANTS","page":"Models","text":"CONVNEXT_VARIANTS :: Dict{Symbol, ConvNeXtVariant}\n\nLookup table for the ConvNeXt v1 variants this package ports: the DINOv3 encoders and the Facebook AI checkpoints from the original ConvNeXt paper. Additional convnext_* lineages (.in12k_*, .clip_*) can be registered without touching the constructor or mapping code.\n\n\n\n\n\n","title":"Luximm.Models.CONVNEXT_VARIANTS"},{"category":"type","location":"api/models/#Luximm.Models.ConvNeXtV2Variant","page":"Models","text":"ConvNeXtV2Variant\n\nArchitectural config for a single ConvNeXtV2 variant.\n\nFields:\n\nname: lookup key (e.g. :convnextv2_atto_fcmae).\ndepths: per-stage block count, (d1, d2, d3, d4).\ndims: per-stage channel widths, (c1, c2, c3, c4). c1 is also the stem output channels. c4 is num_features.\nhf_repo: HuggingFace repo containing model.safetensors.\ndefault_num_classes: head dimension the released weights ship with. 0 for the bare .fcmae encoders, 1000 for the ImageNet-1K and ImageNet-22k-then-1K fine-tunes.\ndefault_input_size: native training resolution (224, 384, or 512) for the released checkpoint. Informational only: the model is fully convolutional and accepts any size, so this is not enforced.\n\n\n\n\n\n","title":"Luximm.Models.ConvNeXtV2Variant"},{"category":"constant","location":"api/models/#Luximm.Models.CONVNEXTV2_VARIANTS","page":"Models","text":"CONVNEXTV2_VARIANTS :: Dict{Symbol, ConvNeXtV2Variant}\n\nLookup table for the ConvNeXtV2 variants this package ports. The .fcmae rows are the bare encoders; all other rows ship a 1000-class ImageNet head. convnextv2_small is not included because timm only registers it as .untrained (no pretrained weights).\n\n\n\n\n\n","title":"Luximm.Models.CONVNEXTV2_VARIANTS"},{"category":"type","location":"api/models/#Luximm.Models.VGGVariant","page":"Models","text":"VGGVariant\n\nArchitectural config for a single VGG variant.\n\nFields:\n\nname: lookup key (e.g. :vgg16_tv_in1k).\ncfg: flat layer list. Each entry is either an Int (a 3x3 pad-1 conv with that output-channel count, followed by ReLU) or the Symbol :M (a 2x2 stride-2 max-pool). Matches timm's cfgs table in vgg.py.\nbatch_norm: whether a BatchNorm sits between every conv and its ReLU (the *_bn checkpoints).\nhf_repo: HuggingFace repo containing model.safetensors.\ndefault_num_classes: head dimension the released weights ship with (1000 for every registered variant).\ndefault_input_size: native training resolution (224). Informational only: the model accepts any size large enough for the 7x7 pre_logits conv, but the released head was trained at 224.\n\n\n\n\n\n","title":"Luximm.Models.VGGVariant"},{"category":"constant","location":"api/models/#Luximm.Models.VGG_VARIANTS","page":"Models","text":"VGG_VARIANTS :: Dict{Symbol, VGGVariant}\n\nLookup table for the VGG variants ported from timm: the four classic depths (11/13/16/19) in plain and BatchNorm flavors, all torchvision tv_in1k checkpoints. Keys are the timm model name with the dot rewritten as an underscore.\n\n\n\n\n\n","title":"Luximm.Models.VGG_VARIANTS"},{"category":"type","location":"api/models/#Luximm.Models.ViTVariant","page":"Models","text":"ViTVariant\n\nArchitectural config for a single Vision Transformer variant.\n\nFields:\n\nname: lookup key (e.g. :vit_base_patch16_224_augreg2_in21k_ft_in1k).\ndepth: number of transformer encoder blocks.\nembed_dim: token / channel width.\nnum_heads: attention heads (head_dim = embed_dim ÷ num_heads).\npatch: patch side length (16).\nimg_size: native input resolution the position embedding was trained at. Enforced by the constructor, since absolute pos-embed has no interpolation path yet.\nhf_repo: HuggingFace repo containing model.safetensors.\ndefault_num_classes: head dimension the released weights ship with.\ndefault_input_size: native training resolution (== img_size).\n\n\n\n\n\n","title":"Luximm.Models.ViTVariant"},{"category":"constant","location":"api/models/#Luximm.Models.VIT_VARIANTS","page":"Models","text":"VIT_VARIANTS :: Dict{Symbol, ViTVariant}\n\nLookup table for the Vision Transformer variants ported from timm. Keys are the timm model name with dots rewritten as underscores.\n\n\n\n\n\n","title":"Luximm.Models.VIT_VARIANTS"},{"category":"type","location":"api/models/#Luximm.Models.CoAtNetVariant","page":"Models","text":"CoAtNetVariant\n\nArchitectural config for a single CoAtNet variant.\n\nFields:\n\nname: lookup key (e.g. :coatnet_0_rw_224_sw_in1k).\ndepths: per-stage block count (d1, d2, d3, d4).\ndims: per-stage output channel widths (c1, c2, c3, c4); c4 is num_features.\nstem_width: the two stem conv widths (s1, s2); s2 feeds stage 1.\nblock_types: per-stage block kind, :C (MBConv) or :T (transformer).\nstride_mode: how MBConv blocks downsample — :pool (avg-pool the main path, stride-1 convs) or :dw (stride the depthwise conv, no pool).\nattn_early: MBConv SE placement — true puts SE between the depthwise conv and norm2 (timm se_early), false after norm2 (timm se).\nse_act: MBConv SE bottleneck activation, :relu or :silu.\ntransformer_shortcut_bias: whether the transformer downsample shortcut's 1x1 expand conv carries a bias.\nlayer_scale: whether transformer blocks apply LayerScale (ls1/ls2 per-channel gamma) to the attention and MLP residual branches.\nimg_size: native input resolution (enforced; the transformer relative-position bias is sized to the per-stage feature map).\nhf_repo: HuggingFace repo containing model.safetensors.\ndefault_num_classes: head dimension the released weights ship with.\ndefault_input_size: native training resolution (== img_size).\n\n\n\n\n\n","title":"Luximm.Models.CoAtNetVariant"},{"category":"constant","location":"api/models/#Luximm.Models.COATNET_VARIANTS","page":"Models","text":"COATNET_VARIANTS :: Dict{Symbol, CoAtNetVariant}\n\nLookup table for the CoAtNet variants ported from timm. Keys are the timm model name with dots rewritten as underscores.\n\n\n\n\n\n","title":"Luximm.Models.COATNET_VARIANTS"}]
}
